# Gallery

Screenshots and GIFs of generated financial models go here.

Suggested captures (add real images — do not fabricate):
- `model-front.png` — the front page: KPI strip, assumptions panel, a month-by-month sheet.
- `analysis-back.png` — the flip side: red flags and the sector-benchmark check.
- `flip.gif` — the model → analysis flip in motion.
- `slider-recompute.gif` — dragging an assumption slider and watching the sheets recompute live.

Once added, reference the hero image from the top of the main [README](../README.md).

> Placeholder only — no images are checked in yet. The human will add real screenshots from a generated artifact.
